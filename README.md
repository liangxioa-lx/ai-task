# AI Task Manager

Tauri + Vite + Vue3 + Pinia 的基础框架。

## 环境要求

- Node.js 20+
- Rust（含 cargo）

## 开发启动

```bash
npm install
npm run tauri dev
```

## 常用命令

```bash
npm run dev
npm run build
npm run tauri dev
```

## 说明

- 当前 `src-tauri/tauri.conf.json` 中 `bundle.active` 设为 `false`，先专注开发调试。
